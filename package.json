{
  "name": "circuitboard-monorepo",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm --prefix web ci --no-audit --no-fund && npm --prefix web run build",
    "start": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview"
  }
}


